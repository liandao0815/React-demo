import styled from 'styled-components';

export const HomeStyle = styled.div`
  width: 200px;
  margin: 200px auto;
  display: flex;
  justify-content: space-between;

  > button {
    cursor: pointer;
  }
`;
